# Independent QA iteration 2: upstream format fix

Date: 2026-08-27  
Verdict: **FAIL**

No commit, push, deploy, production write, or container lifecycle action occurred. QA used only disposable local PostgreSQL 17 databases and the public audited upstream Git tree.

## Release blocker

The discovery rule does not implement the required spring/construction exclusions. `classifyCatalogTree()` accepts every path with either standard suffix before considering its directory or semantic class, and it explicitly allowlists the construction export.

At exact upstream revision `66cc5aa36208bb33997d3a037137ff60885f5861`, independent execution of `fetchThereminGoatCatalog()` returned:

- 5,420 catalog candidates: 2,757 `RAW_DATA`, 2,650 `HIGH_RESOLUTION_RAW`, 13 `NONSTANDARD_REVIEW`.
- **68 admitted candidates whose path contains `SwitchOddities Spring Testers`**. These use standard-looking suffixes but are spring/component measurements, not switch curves.
- `Keyfirst Bling Green/Keyfirst Bling Green Data Construction.csv` is also admitted as `NONSTANDARD_REVIEW` despite the explicit construction exclusion gate.
- The two nonstandard spring CSVs are excluded, but excluding only those two is insufficient.

This is fail-closed at the MasterSwitch mapping boundary because upstream candidates lack verified manufacturer/technology metadata, so no unsafe automatic mapping was created. It still fails the catalog acceptance rule and would add invalid spring/construction artifacts and review cases to production.

Required correction: add explicit deterministic exclusion before suffix/allowlist inclusion for the audited spring-tester subtree and construction artifacts (including removal of the construction path from the allowlist), add exact regression fixtures, then rerun this QA suite. Do not broaden filename matching.

## Independently passing evidence

### Clean migration and PostgreSQL fixture

Disposable database `qa_fc_format_fix_ind`:

- `npx prisma migrate deploy`: PASS, all 33 migrations.
- `npm run test:force-curves-db`: PASS (`runs=7`, `catalog=10`, `reviews=11`, `peachApprovedUrls=[]`).
- Repeated `prisma migrate deploy`: PASS/no pending migrations.
- `prisma migrate status`: database schema up to date.

The fixture verifies the prior completed zero-catalog revision can be repaired by a distinct `66cc5aa:formats-v2` run key, exact source path/hash/catalog revision persistence, grouped high-resolution-first pair payloads, review-only legacy data, same-revision idempotency, incremental revision reuse, and Peach Blossom zero approved URLs.

### Live upstream packaged-runner execution

Disposable database `qa_fc_format_live_ind`, migrated from scratch:

- First packaged runner execution: `COMPLETED`, cursor/after/new `5420`, errors `0`, reviews `2798`.
- Second execution returned the exact same run ID and counts; no duplicate sync run or reviews.
- Persisted open reviews: `SOURCE_UNVERIFIED=2785`, `SOURCE_NONSTANDARD=13`; approved mappings `0`.
- Immutable identity retained the exact revision and Git blob SHA. The run revision is `66cc5aa...:formats-v2`, while catalog entries retain exact upstream revision `66cc5aa...`.
- Group audit: 2,798 measurement groups, 2,622 two-member groups, no group larger than two at this revision, high-resolution priority encoded by the review payload ordering.

Builder evidence expected 2,795 reviews, but independent execution consistently produced 2,798. The implementation-derived count is deterministic, but the evidence expectation must be corrected and then recalculated after invalid spring/construction artifacts are excluded.

### Production-shaped review reconciliation

QA inserted 2,756 production-like legacy `UNMATCHED/OPEN` rows (each attached to an approved fixture MasterSwitch), then ran a new corrected-format revision:

- All 2,756 rows were retained and transitioned to `RESOLVED/NO_MATCH`; zero were deleted.
- Exact row count with the fixture IDs remained 2,756.
- Existing 2,798 source review rows remained open with no duplicate explosion.
- Reconciliation run: before/after `5420`, new/changed/stale `0`, errors `0`; approved mappings remained `0`.
- Repeating a completed revision remains a no-op by stable run identity.

The reconciliation is idempotent and auditable for the production noise shape. Static inspection notes that it resolves all pre-run open `UNMATCHED` rows, so the intended scope relies on all legacy `UNMATCHED` records being mechanically generated by the retired matcher; feedback uses the distinct `FEEDBACK` kind.

## Project gates

- `npm test`: PASS, 58/58.
- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS with pre-existing warnings only.
- `npm run build`: PASS, 82 static pages.
- `npm run build:force-curves-sync`: PASS, 13.2 KB CommonJS bundle.
- `git diff --check`: PASS.
- Ops evidence diff inspection: only prior appended release evidence plus whitespace normalization on the original header lines; no semantic deletion. This evidence file remains feature-owned, while unrelated dirty files were untouched.

## Fail-closed matching audit

- Automatic selection requires `exists`, `metadataVerifiedAt`, non-null manufacturer and technology, exact normalized manufacturer+switch identity, manufacturer equality, and technology equality.
- Absent/conflicting metadata therefore cannot auto-approve; multiple verified candidates become `AMBIGUOUS` review.
- Manual approvals/rejections outrank automation; changed approved content becomes `STALE`; disappeared paths become stale.
- Read path exposes only approved canonical mappings and `NO_MATCH` takes precedence.
- KTT Peach Blossom `cmqo21sm103vknu3vh0tjs75x` remained zero approved URLs and cannot match Peach Sun/Cherry Blossom/Jerrzi Cherry Blossom without verified exact metadata.

## Verdict

**FAIL.** Migration safety, packaging, idempotency, reconciliation, matching fail-closed behavior, Peach regression, and project gates pass independently. Production release must remain stopped until spring-tester and construction artifacts are excluded and the corrected catalog/review counts and regression tests pass another independent QA iteration.
